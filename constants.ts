export const PRODUCTS_PER_PAGE = 12;

export const PRICE_RANGE_MINIMAL_VALUE = 100;

export const PRICE_RANGE_MAXIMAL_VALUE = 5000;

const PRICE_RANGE_STEP = 10;

export const CURRENCY = '$';

export const SLIDER_CONFIG = {
    minValue: PRICE_RANGE_MINIMAL_VALUE,
    maxValue: PRICE_RANGE_MAXIMAL_VALUE,
    step: PRICE_RANGE_STEP
};
